
enum RoleEnum {
    ADMIN = 'admin',
    USER = 'user',
    GUEST = 'guest'
}

export class CreateRoleDto {
    // email: string;
    // password: string;
    // roles: RoleEnum[] = [];
    // isEnabled?: boolean = true;
}
